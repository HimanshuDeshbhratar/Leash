JWT_SECRET=leash-demo-secret-change-me-in-real-use
PORT=3000

# On Vercel: set JWT_SECRET in Project Settings → Environment Variables.
# Node 22+ is required (engines field) for the built-in node:sqlite module.
